import postImage from '../static/postImage.svg'

let initialState = {
    myPosts: [],
    allPosts: [
        {id: 1, text: 'First Post', image: postImage}
    ]
}

const postsReducer = (state: any, action: any) => {

}

export default postsReducer